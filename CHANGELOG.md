# 变更记录

本文档记录配置设计的主要历史决策。当前运行行为以 [`meta.ini`](meta.ini) 和 [OPENCLASH_CONTEXT.md](OPENCLASH_CONTEXT.md) 为准。

## 2026-08-29

- 增加日本原生解锁组，覆盖 Pixiv、DLsite、FANZA、Fantia 和 DMM 相关域名。
- 增加 Reddit 专属规则和台湾默认出口。
- 增加 Google Voice 专属规则和美国默认出口。

## 2026-08-02

- 调整基础策略组和应用策略组的默认节点选择。

## 2026-08-01

- Sub-Store 节点整理脚本增加连接参数去重，避免同一节点因展示名称不同而重复出现。

## 2026-07-26

- 修复国家节点组的误匹配问题，尤其是裸国家代码造成的跨国家误归类。
- 将国家节点故障切换统一为 `fallback`，自动选择组保留测速逻辑。
- 将高倍率定义统一为严格大于 `1x` 或 `1倍`。
- 确定 `meta.ini` 为 OpenClash 的唯一规则与策略组维护源。
- 删除并停止维护旧的 `maoxiong_rules.toml` 方案。

## 2026-07-22

- 增加 Sub-Store 节点地区和倍率排序脚本。
- 完善本地 SOCKS 转换工具的地区端口分配。

## 2026-07-18

- 将 `0.1x` 的处理范围限制在约定的核心国家普通组，不再以宽泛的福利标签作为全局删除依据。
- 增加 Sub-Store 核心节点筛选脚本。

## 2026-07-14

- 完善 AI、Google FCM、游戏、下载、交易所和基础出口策略。
- 调整应用策略组默认值和全局出口结构。

## 已废弃方案

- `maoxiong_rules.toml`：曾与 `meta.ini` 并行维护，已废弃。
- `openclash-sub/`：曾用于通过 Docker/Nginx 暴露私有节点文件，已删除。
- 私有节点补充文件：不再作为仓库内的 OpenClash 规则源。
